# Use DSL as the V2 executable program format

V2 research experiments will accept model-generated programs only in the project DSL and will parse them into ASTs before execution. Arbitrary model-generated Python remains available only through the V1 baseline adapter and cannot feed the V2 memory experiments, providing enforceable syntax, Normalized AST comparison, bounded actions, and safer evaluation.
